// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility that Flutter provides. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

// import 'package:flutter_test/flutter_test.dart';
// import 'package:rick_and_morty_test_app/screens/characters_screen/characters_screen/local_widgets/box_character_data_widget.dart';

void main() {
  // testWidgets('Text color to isAlive value', (tester) async{
  //   await tester.pumpWidget(const BoxCharacterDataWidget(name: 'Rick Sanchez', isAlive: 'Alive', gender: 'Male', species: 'Human', function: (){returnlk},));
  //   final nameFinder = find.text('Rick Sanchez');
  //   final isAliveFinder = find.text('Alive');
  //   final genderFinder = find.text('Male');
  //   final speciesFinder = find.text('Human');

  //   expect(nameFinder, findsOneWidget);
  //   expect(isAliveFinder, findsOneWidget);
  //   expect(genderFinder, findsOneWidget);
  //   expect(speciesFinder, findsOneWidget);
  // });
}
